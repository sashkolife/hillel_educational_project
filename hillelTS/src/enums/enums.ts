export enum Pages {
    HOME = "/",
    AUTH = "/auth",
    ABOUT = "/about",
    USERS = "/users",
    POSTS = "/posts",
    USER_PROFILE = "/users/:id",
    PAGE_NOT_FOUND = "/*"
}

export enum UserLevel {
    BEGINNER = "beginner",
    PROFESSIONAL = "professional",
    MASTER = "master"
}